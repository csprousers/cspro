Run these batch scripts from a Visual Studio command prompt.

zlib must be run before curl and libxlsxwriter.

The curl scripts must be run in a Visual Studio command prompt matching x86/x64.

The libxlsxwriter script is no longer used because it is built as part of the CSPro
solution but is kept around for reference.
